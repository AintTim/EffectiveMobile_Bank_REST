# 🏦 Система управления банковскими картами

**REST API микросервис** для управления банковскими картами, пользователями и финансовыми операциями. Приложение обеспечивает безопасное хранение данных, авторизацию пользователей и выполнение банковских операций.

## ✨ Основные возможности

### 👥 Управление пользователями
- Регистрация новых пользователей с валидацией данных
- Аутентификация через JWT (access/refresh токены)
- Управление ролями (USER, ADMIN)
- Смена пароля с подтверждением

### 💳 Управление банковскими картами
- Создание и удаление карт (только для администраторов)
- Просмотр списка карт с пагинацией и сортировкой
- Получение детальной информации о карте
- Проверка баланса
- Блокировка карты владельцем

### 🔄 Финансовые операции
- Переводы между картами одного пользователя
- Проверка достаточности средств
- Защита от некорректных операций (блокированные карты, одинаковые счета)

### 🔐 Безопасность
- JWT аутентификация с refresh токенами
- HTTP-only cookies для защиты refresh токенов
- Маскировка номеров карт при выводе (XXXX XXXX XXXX 5678)
- Проверка прав доступа на уровне API и бизнес-логики
- Защита от SQL-инъекций, XSS

## 🛠 Технологический стек
| Технология | Назначение |
|------------|------------|
| **Java 17** | Основной язык разработки |
| **Spring Boot 3.5.6** | Фреймворк для создания приложения |
| **Spring Security** | Аутентификация и авторизация |
| **Spring Data JPA** | Работа с базой данных |
| **PostgreSQL 15** | Основная база данных |
| **Liquibase** | Миграции базы данных |
| **JWT (jjwt)** | Токены для аутентификации |
| **Docker & Docker Compose** | Контейнеризация и оркестрация |
| **SpringDoc OpenAPI 3** | Документация API (Swagger) |
| **JUnit 5 & Mockito** | Модульное и интеграционное тестирование |
| **MapStruct** | Маппинг DTO ↔ Entity |
| **Lombok** | Уменьшение boilerplate кода |
| **Hibernate Validator** | Валидация входных данных |

## 🚀 Быстрый старт

### Предварительные требования
- Docker & Docker Compose
- Java 17+ (для локальной разработки)
- Git

### Установка и запуск через Docker (рекомендуется)

1. **Клонируйте репозиторий**
   ```bash
   git clone https://github.com/AintTim/EffectiveMobile_Bank_REST.git
   cd effectivemobile_bank_rest
    ```

2. Настройте переменные окружения
- Переименуйте файл `.env.example` в `.env`
- Сгенерируйте случайный JWT ключ

3. Запустите приложение

    ```
    # Сборка и запуск
    docker-compose up --build -d
    
    # Проверка статуса
    docker-compose ps
    
    # Просмотр логов
    docker-compose logs -f app
    ```

4. Приложение доступно по адресам:
    ```
    API: http://localhost:8080
    
    Swagger UI: http://localhost:8080/swagger-ui.html
    
    PostgreSQL: localhost:5432
    ```

## 📖 Документация API
Полная документация доступна через Swagger UI после запуска приложения:

## Пример использования API

1. Создайте пользователей
    ```
    POST http://localhost:8080/api/users
    ```
**Пример тела запроса:**
```
{
    "name": "Bob",
    "email": "bob@example.com",
    "password": "1qaz2wsx",
    "role": "ADMIN" или "USER"
}
```

2. Авторизуйтесь пользователем с правами администратора для получения токена доступа
    ```
    POST http://localhost:8080/api/auth/login
    ```
**Пример тела запроса:**
```
{
    "email": "bob@example.com",
    "password": "1qaz2wsx"
}
```

3. Создайте банковские карты для пользователей
    ```
    POST http://localhost:8080/api/cards
    ```
**Пример тела запроса:**
```
{
    "number": "1111222233334444",
    "userId": "1",
    "expirationDate": "11.11.2029",
    "balance": 500
}
```

4. Получите список всех карт
    ```
    GET http://localhost:8080/api/cards
    ```

5. Авторизуйтесь пользователем, для которого ранее создали карты и сделайте перевод средств между его картами
    ```
    POST http://localhost:8080/api/cards/transfer
    ```
**Пример тела запроса:**
```
{
    "fromCard": "a4acd01e-b88c-4d17-8352-a85c9f76ba1c", ## id карты, с которой отправляем средства
    "toCard":  "8f610915-5e32-4b3e-b24e-4f00f536bdbe",  ## id карты, на которую поступят средства
    "amount": 200
}
```
